import { Note } from "../../types/Note"

interface FormProps {
    note: Note
}

export default function Form({note} : FormProps) { //заглушка в коде
    return (
        <>
            
        </>
    )
}